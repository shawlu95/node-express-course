## Highlights
* Custom middleware
* Error handling
* Use MongoDB atlas
* Use dotenv

## Note
* Project is not deployed
* No authentication

```bash
npm install
npm start

# run on custom port (deploy on server)
PORT=6000 node app.js
```